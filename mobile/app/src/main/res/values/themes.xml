<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AuthappMobile" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorPrimaryVariant">@color/primary_purple</item>
        <item name="colorSecondary">@color/accent_blue</item>
        <item name="colorError">@color/error_text</item>
        <item name="android:windowBackground">@color/bg_light</item>
    </style>

    <style name="Theme.AuthappMobile" parent="Base.Theme.AuthappMobile" />

    <!-- Auth Box Shadow -->
    <style name="AuthBoxStyle">
        <item name="android:elevation">8dp</item>
    </style>

    <!-- Button Gradient Style -->
    <style name="PrimaryButton">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingStart">24dp</item>
        <item name="android:paddingEnd">24dp</item>
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
    </style>
</resources>